# Tasks: Documents, Completion, Billing Readiness, Rates, and Export

**Input**: Design documents from `/specs/008-documents-billing-export/`
**Prerequisites**: plan.md (required), spec.md (user stories US1–US5), research.md (R0–R15), data-model.md (7 tables / 3 enums / no trips ALTER / gate evaluators + billing compute / services / read models / migration 0007), contracts/ (bff-endpoints.md, permission-matrix.md), quickstart.md
**Tests**: INCLUDED — explicitly required by the plan's Testing section, Constitution §3.13 (the pure completion/billing-readiness gates + billing-value computation, permission checks, the worker jobs), and quickstart.md.

**Organization**: Tasks are grouped by user story so each story is independently implementable and testable. Setup → Foundational (blocking) → US1 (P1) → US2 (P1) → US3 (P2) → US4 (P2) → US5 (P2) → Polish.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependency on an incomplete task)
- **[Story]**: US1..US5 for story-phase tasks (Setup / Foundational / Polish have no story label)
- Exact repo-relative file paths are in each description

## Path Conventions

Existing monorepo (per plan.md Project Structure): `packages/shared/src/`, `packages/db/{schema,src,migrations,seed}/`, `workers/`, `apps/web/`. **Seven new tables** (`document_types`, `documents`, `document_requirements`, `rates`, `billing_items`, `billing_adjustments`, `export_batches`); **three new pgEnums** (`document_verification_status`, `export_batch_status`, `billing_adjustment_type`); `export_batches.format`/`billing_items.dispute_status` are CHECK text; `document_types` is a config table; **no `trips` ALTER** (billing status is the `billingStatus(current_status)` projection — FR-011); **no new permission key, package, worker process, or runtime dependency** (ExcelJS emits CSV+XLSX). Completion/billing-readiness authority is server-side (pure evaluators the BFF calls); binaries in Supabase Storage (metadata in Postgres, served via signed URLs); freshness is polling; UI is pt-BR; timestamps UTC; money integer centavos (BRL).

---

## Phase 1: Setup

**Purpose**: Branch off `dev` for the slice.

- [X] T001 Create the short-lived feature branch `008-documents-billing-export` off `dev` (`git checkout -b 008-documents-billing-export dev`)

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: The shared domain gates + billing compute + Zod + audit/jobs contracts, the seven-table schema + migration 0007, the Storage helpers, the cross-cutting service reads/helpers (`resolveRequiredTypes`, `resolveRate`, `ensureBillingItem`, `loadBillingItemView`) + read-model extensions, the extended read routes, i18n/nav, the worker-queue wiring + env, the seeds, and the pure unit tests that ALL user stories depend on.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

### Shared (`@brazil-tms/shared`)

- [X] T002 [P] Create the pure documents module in `packages/shared/src/domain/documents.ts`: `DOCUMENT_VERIFICATION_STATUSES = ['pending_review','accepted','rejected'] as const`, `DEFAULT_DOCUMENT_CHECKLIST` (labeled scaffolding, e.g. `[{ typeCode: 'pod', requiredForCompletion: false, requiredForBilling: true }]`), the `RequiredType` type, and `evaluateChecklist(required, satisfiedTypeIds): { completionMissing: string[]; billingMissing: string[] }` — per data-model §9.1
- [X] T003 [P] Create the pure billing module in `packages/shared/src/domain/billing.ts`: `BILLING_ADJUSTMENT_TYPES` (the 7 types), `EXPORT_FORMATS = ['csv','xlsx']`, `EXPORT_BATCH_STATUSES = ['queued','running','completed','failed']`; `computeBillingValues(baseFreightCents, adjustments): { plannedCents, executedCents, adjustmentCents, finalBillableCents }` (discounts subtracted, `manual_adjustment` signed, executed = base freight, final = executed + adjustments); and the two **server-authoritative gate evaluators** `evaluateCompletionReadiness(ctx): { canComplete; blockers: CompletionBlocker[] }` (§19.3 — `not_unloaded`/`missing_completion_documents`/`cancelled`) and `evaluateBillingReadiness(ctx): { canBillReady; blockers: BillingBlocker[] }` (§19.4 — `not_billing_pending`/`missing_billing_documents`/`no_pricing`/`open_billing_dispute`) — per data-model §9.2 (mirror `domain/sla-risk.ts`)
- [X] T004 [P] Create the billing job contract in `packages/shared/src/billing/jobs.ts`: `BILLING_JOBS = { billingExport: 'billing.export' } as const`, `BillingJobName`, `BillingExportPayload { exportBatchId; actorUserId }`, `BillingJobPayloads` — pure, no pg-boss import (sibling of `import/jobs.ts`/`sla/jobs.ts`), per data-model §9.3
- [X] T005 [P] Create the documents job contract in `packages/shared/src/documents/jobs.ts`: `DOCUMENT_JOBS = { documentChecks: 'documents.checks' } as const`, `DocumentJobName`, `DocumentChecksPayload` (empty — scheduled), `DocumentJobPayloads` — per data-model §9.3
- [X] T006 [P] Extend the `AuditAction` union AND `ALL_AUDIT_ACTIONS` in `packages/shared/src/audit/actions.ts` with `document.upload` / `document.verify` / `document.waive` / `document.archive` / `document_requirement.create` / `document_requirement.update` / `document_type.create` / `document_type.update` / `rate.create` / `rate.update` / `billing_item.update` / `billing.export` (keep both in lockstep so the `satisfies` check passes; completion/Billing-Ready/Billed reuse the existing `trip.status_change`) — per data-model §9.4 / R12
- [X] T007 [P] Create `packages/shared/src/schemas/document.ts` exporting `uploadDocumentMetaSchema` (`documentTypeId` uuid, `externalReference?` ≤200, `notes?` ≤2000, `fileName` min 1) and `verifyDocumentSchema` (`verificationStatus` `z.enum(DOCUMENT_VERIFICATION_STATUSES)`, `notes?`) — per data-model §10 / contracts §1–2
- [X] T008 [P] Create `packages/shared/src/schemas/document-requirement.ts` exporting `createDocumentRequirementSchema`/`updateDocumentRequirementSchema` (`customerId` uuid, `documentTypeId` uuid, `requiredForCompletion`/`requiredForBilling` booleans, optional `laneId`/`vehicleType`, `active`) and `createDocumentTypeSchema`/`updateDocumentTypeSchema` (`code` min 1, `labelPt` min 1, `active`, `sortOrder?`) — per data-model §10 / contracts §7–10
- [X] T009 [P] Create `packages/shared/src/schemas/rate.ts` exporting `createRateSchema`/`updateRateSchema` (`customerId` uuid; optional `laneId`/`vehicleType`; `baseAmountCents` `z.number().int().nonnegative()`; `currency` default `'BRL'`; optional `tollHandlingRule`/`waitingTimeRule`/`extraStopRule` text; `effectiveStart`/`effectiveEnd` `z.coerce.date().optional()`; `active`) — per data-model §10 / contracts §11–12
- [X] T010 [P] Create `packages/shared/src/schemas/billing.ts` exporting `addBillingAdjustmentSchema` (`type` `z.enum(BILLING_ADJUSTMENT_TYPES)`, `amountCents` `z.number().int()`, `note?` ≤500), `updateBillingItemSchema` (`baseFreightCents?` int, `billingPeriod?` `/^\d{4}-\d{2}$/`, `disputeStatus?` `z.enum(['none','open','resolved'])`, `notes?`), `markCompletedSchema`/`markBillingReadySchema` (`waivedRequirements?: { documentTypeId: uuid, reason: string }[]`), and `createExportSchema` (`customerId` uuid, `billingPeriod` `/^\d{4}-\d{2}$/`, `format` `z.enum(EXPORT_FORMATS)`) — per data-model §10 / contracts §5–6, §13–16
- [X] T011 [P] Extend `packages/shared/src/schemas/trip-board.ts`: add a `missingDocuments` filter param (`optParam(z.enum(["true","false"]))`) for the "Missing documents" view and add its key to `PARAM_KEYS` (a param missing from `PARAM_KEYS` silently never parses) — per data-model §10 / contracts §21
- [X] T012 Add `export *` lines to `packages/shared/src/index.ts` for `./domain/documents`, `./domain/billing`, `./billing/jobs`, `./documents/jobs`, `./schemas/document`, `./schemas/document-requirement`, `./schemas/rate`, `./schemas/billing` (after the existing 007 lines) (depends on T002, T003, T004, T005, T007, T008, T009, T010)

### Database schema + migration (`@brazil-tms/db`)

- [X] T013 Extend `packages/db/schema/enums.ts`: add the three `pgEnum`s `documentVerificationStatus` (`pending_review/accepted/rejected`), `exportBatchStatus` (`queued/running/completed/failed` — mirrors `importBatchStatus`), `billingAdjustmentType` (the 7 types) — per data-model §1
- [X] T014 [P] Create `packages/db/schema/document-types.ts`: the `document_types` pgTable (`code` unique, `label_pt`, `active`, `sort_order`, timestamps) — config master, mirrors `reason-codes.ts`, per data-model §2
- [X] T015 Create `packages/db/schema/documents.ts`: the `documents` pgTable (inline FKs to trips/document_types/users×3; `file_storage_key` **nullable**; `external_reference`; `verification_status` enum default `pending_review`; `verified_by_user_id`/`verified_at`; the waiver cols `waived_at`/`waived_reason`/`waived_by_user_id`; `notes`; `archived_at` soft-delete; timestamps) with the `documents_file_or_waiver_ck` CHECK (`file_storage_key IS NOT NULL OR waived_at IS NOT NULL`) and the `trip`/`type`/`verification` indexes — per data-model §3 (depends on T013, T014)
- [X] T016 [P] Create `packages/db/schema/document-requirements.ts`: the `document_requirements` pgTable (FK `customer_id`/`document_type_id`; `required_for_completion`/`required_for_billing` booleans; nullable scope `lane_id` FK + `vehicle_type` reusing the existing `vehicleType` enum; `active`; timestamps) with `document_requirements_customer_idx` + the composite `document_requirements_scope_idx (customer_id, lane_id, vehicle_type)` — per data-model §4 (depends on T013, T014)
- [X] T017 [P] Create `packages/db/schema/rates.ts`: the `rates` pgTable (FK `customer_id`; nullable `lane_id` FK + `vehicle_type`; `base_amount_cents` `bigint('…',{mode:'number'})`; `currency` default `'BRL'`; nullable `toll_handling_rule`/`waiting_time_rule`/`extra_stop_rule` text — §29 Input #5, not interpreted; `effective_start`/`effective_end`; `active`; timestamps) with `rates_customer_idx` + `rates_scope_idx (customer_id, lane_id, vehicle_type)` — per data-model §5 (depends on T013)
- [X] T018 [P] Create `packages/db/schema/export-batches.ts`: the `export_batches` pgTable (FK `customer_id`/`generated_by_user_id`; `billing_period` text; `format` text + `export_batches_format_ck` CHECK (`'csv','xlsx'`); `file_storage_key` nullable; `status` `exportBatchStatus` enum default `queued`; `trip_count` int; `total_amount_cents` bigint; `error_message`; timestamps) with `export_batches_customer_idx` + `export_batches_created_idx (created_at desc)` — mirrors `import-batches.ts`, per data-model §7 (depends on T013)
- [X] T019 Create `packages/db/schema/billing-items.ts`: the `billing_items` pgTable (FK `trip_id`/`customer_id`; nullable `rate_id` FK; `base_freight_cents` bigint nullable; `currency` default `'BRL'`; `billing_period` text NOT NULL; `dispute_status` text default `'none'` + `billing_items_dispute_status_ck` CHECK; nullable `export_batch_id` FK→export_batches; `notes`; timestamps) with the **unique** `billing_items_trip_uq (trip_id)` + `billing_items_customer_period_idx (customer_id, billing_period)` + `billing_items_export_batch_idx` — per data-model §6 (depends on T013, T018)
- [X] T020 Create `packages/db/schema/billing-adjustments.ts`: the `billing_adjustments` pgTable (FK `billing_item_id`; `type` `billingAdjustmentType` enum; `amount_cents` bigint; `note`; `created_by_user_id` FK; `created_at`; the **soft-remove** cols `removed_at`/`removed_by_user_id`) with `billing_adjustments_item_idx` — per data-model §6 (depends on T013, T019)
- [X] T021 Add `export * from "./document-types"` / `"./documents"` / `"./document-requirements"` / `"./rates"` / `"./export-batches"` / `"./billing-items"` / `"./billing-adjustments"` to `packages/db/schema/index.ts` (after the 007 `alerts` line) (depends on T014, T015, T016, T017, T018, T019, T020)
- [X] T022 Generate + apply Drizzle migration `packages/db/migrations/0007_*.sql` via `pnpm --filter "@brazil-tms/db" db:generate` then `db:migrate` (next sequential after `0006_large_carmella_unuscione.sql`): `CREATE TYPE`×3 + `CREATE TABLE`×7 (with FKs, the `documents_file_or_waiver_ck`/`billing_items_dispute_status_ck`/`export_batches_format_ck` CHECKs, the `billing_items_trip_uq` unique, all indexes). **No `trips` ALTER** (billing status is the projection). **NO REVOKE** for the seven mutable tables; `trip_events`/`audit_logs` keep their REVOKE. **Hand-verify** before applying: (a) `CREATE TYPE`×3 ordered before first use; (b) the `documents_file_or_waiver_ck` two-column CHECK; (c) `billing_items_trip_uq` UNIQUE on `trip_id`; (d) `export_batches` created **before** `billing_items` so the `export_batch_id` FK resolves; (e) `export_batches.format`/`billing_items.dispute_status` CHECK text. Verify `meta/_journal.json` updated (depends on T013, T021)
- [X] T023 [P] Create `packages/db/seed/document-types.ts` + a `db:seed:document-types` package script: the 5 **labeled-scaffolding** types (`pod`, `cte`, `mdfe`, `gate_receipt`, `portal_ref`) with pt-BR labels — explicitly NOT final business sign-off (Constitution II; mirrors 007's reason-code seed) — per data-model §15 / quickstart (depends on T022)
- [X] T024 [P] Create `packages/db/seed/rates.ts` + a `db:seed:rates` package script: one example `rates` row (so a completed trip for that customer auto-prices and Billing Ready is demonstrable; customers with no matching rate use a manual amount + billing-rule sign-off blocked) — per data-model §15 / quickstart (depends on T022)

### Storage helpers, cross-cutting service reads + read models (`@brazil-tms/db`)

- [X] T025 [P] Extend `packages/db/src/storage.ts`: add `documentsBucket()` (env `DOCUMENTS_BUCKET ?? 'documents'`), `exportsBucket()` (env `EXPORTS_BUCKET ?? 'billing-exports'`), `documentStorageKey(tripId, documentId, ext)` (→ `trips/${tripId}/${documentId}.${ext}`), `exportStorageKey(exportBatchId, ext)` (→ `exports/${exportBatchId}.${ext}`), `putDocument(key, bytes, contentType)`, `putExport(key, bytes, contentType)`; reuse the existing `downloadObject`/`signedUrl` (the shared service-role client serves app + worker — no new client) — per data-model §8 / research R8 (depends on T001)
- [X] T026 Create `packages/db/src/documents/requirements.ts` exporting the foundational reads `resolveRequiredTypes(trip): RequiredType[]` (the applicable `document_requirements` rows by customer + optional lane/vehicle-type scope, additive; **no rows for the customer ⇒ `DEFAULT_DOCUMENT_CHECKLIST` + the customer reported document-checklist sign-off blocked**), `listDocumentRequirements(customerId?)`, and `listDocumentTypes()` (CRUD lands in US3) — per data-model §11 / R2 (depends on T022, T012)
- [X] T027 Create `packages/db/src/billing/rates.ts` exporting the foundational read `resolveRate(trip): Rate | null` (the precedence query `ORDER BY (lane_id IS NOT NULL) DESC, (vehicle_type IS NOT NULL) DESC, effective_start DESC NULLS LAST LIMIT 1` over active, in-effective-window rows; null when none) + `listRates(filters)` (CRUD lands in US4) — per data-model §11 / R4 (depends on T022, T012)
- [X] T028 Create `packages/db/src/billing/billing-items.ts` exporting the foundational helpers `ensureBillingItem(tx, tripId)` (insert one `billing_items` per trip if absent — `billing_period` = the calendar month of completion in `America/Sao_Paulo`, `base_freight_cents` from `resolveRate(trip)` or null, `rate_id` accordingly) and `loadBillingItemView(tx, tripId)` (item + **live** (`removed_at IS NULL`) adjustments + `computeBillingValues` → planned/executed/adjustment/final + `missingCompletionDocuments`/`missingBillingDocuments` summaries) (the `updateBillingItem`/adjustment mutations land in US4) — per data-model §11 / R5/R7 (depends on T022, T012, T027)
- [X] T029 Extend `packages/db/src/trips/trip-dto.ts` / `loadTripDetail`: define `DocumentDto` + `BillingItemView`; add `documents: DocumentDto[]` (non-archived rows + type code/label, verification, waiver, external ref + the missing-required-document summary) and `billing: BillingItemView | null` (via `loadBillingItemView`) to `TripDetail`, loaded in the **same** `loadTripDetail` executor (fills the 005 placeholders; single source) — per data-model §13 / R13 (depends on T028, T026)
- [X] T030 Extend `packages/db/src/trips/trips-read.ts`: fill `completedMissingDocuments` in `queryDashboardMetrics` (count of billing-phase trips with ≥1 unmet required-for-billing document — replaces the `null` placeholder); add the `missingDocuments` board filter to `buildWhere`; add the reads `queryBillingList({ scope:'pending'|'ready', customerId?, billingPeriod? })` (FR-019 — reuse the `boardSelect` join shape filtered on the `billingStatus` projection + the computed final value + a missing-proof indicator), `queryExportBatches(customerId?, billingPeriod?)`, `queryRates`/`queryDocumentRequirements`/`queryDocumentTypes` (admin lists) — per data-model §13 / R13 (depends on T022, T028, T026)
- [X] T031 Extend `packages/db/src/index.ts`: export the storage helpers (`putDocument`/`putExport`/`documentStorageKey`/`exportStorageKey`/`documentsBucket`/`exportsBucket`), the foundational reads/helpers (`resolveRequiredTypes`/`listDocumentRequirements`/`listDocumentTypes`, `resolveRate`/`listRates`, `ensureBillingItem`/`loadBillingItemView`), and the read-model functions + new types (`DocumentDto`/`BillingItemView`, `queryBillingList`/`queryExportBatches`/`queryRates`/`queryDocumentRequirements`/`queryDocumentTypes`) (depends on T025, T026, T027, T028, T029, T030)

### Web foundation — Storage re-export, extended read routes, nav, i18n (`apps/web`)

- [X] T032 [P] Extend `apps/web/lib/supabase/storage.ts` (the `"server-only"` boundary): re-export the new `putDocument`/`putExport`/`documentStorageKey`/`exportStorageKey`/`documentsBucket`/`exportsBucket` from `@brazil-tms/db/storage` (depends on T025)
- [X] T033 [P] Create `apps/web/lib/billing/queue.ts`: `enqueueBillingExport(payload)` (a lazily-started pg-boss instance for the `billing.export` queue, mirroring `apps/web/lib/imports/queue.ts` `getBoss()`/`enqueueImportJob`) — per data-model §12 (depends on T004)
- [X] T034 [P] Update `apps/web/lib/trips/trips-read.ts` and `apps/web/lib/trips/trip-dto.ts` (server-only) to re-export the extended read models + new types (`DocumentDto`/`BillingItemView`, `queryBillingList`/`queryExportBatches`/`queryRates`/`queryDocumentRequirements`/`queryDocumentTypes`, `resolveRequiredTypes`/`resolveRate`/`ensureBillingItem`/`loadBillingItemView`) from `@brazil-tms/db` (depends on T031)
- [X] T035 [P] Append the `"missing_documents"` preset to `DEFAULT_TRIP_VIEWS` in `apps/web/lib/trips/views.ts` (`params` → `missingDocuments:"true", scope:"active"`; `labelKey:"viewMissingDocuments"`) — the slot 005 reserved (depends on T011)
- [X] T036 [P] Extend `apps/web/app/api/trips/route.ts` (GET board, stays `view_all_trips`): parse + forward the new `missingDocuments` filter param (depends on T034)
- [X] T037 [P] Extend `apps/web/app/api/trips/[id]/route.ts` (GET detail, stays `view_all_trips`): return `documents[]` + `billing` in the `TripDetailView` (depends on T034)
- [X] T038 [P] Extend `apps/web/app/api/dashboard/summary/route.ts` (GET, stays `view_all_trips`): return the filled `completedMissingDocuments` metric (`billingPendingCount` already ships live in 005 — unchanged) (depends on T034)
- [X] T039 [P] Extend `NAV_ITEMS` in `apps/web/lib/nav.ts`: add **Documents** (`href:"/documents"`, permission `view_all_trips`), **Billing** (`href:"/billing"`, permission `view_all_trips`), and **Rates** (`href:"/billing/rates"`, permission `edit_rates`) (depends on T001)
- [X] T040 [P] Extend `apps/web/messages/pt-BR.json`: `Documents` / `Billing` / `Rates` namespaces (upload/verify/missing-list, billing values/adjustments/complete/billing-ready, rate admin, document-requirement/type admin, export + history), the "Missing documents" view + filter labels under `Trips.board` (`viewMissingDocuments`, `filterMissingDocuments`), and the **twelve** new audit actions BOTH nested under `Trips.auditActions` (`document.upload/verify/waive/archive`, `document_requirement.create/update`, `document_type.create/update`, `rate.create/update`, `billing_item.update`, `billing.export`) AND flat under `AuditActions` (`document_upload`/…/`billing_export`) — **no dotted keys** (depends on T006)

### Worker foundation (`@brazil-tms/workers`) + env

- [X] T041 Extend `workers/lib/queue.ts`: merge `BILLING_JOBS` + `DOCUMENT_JOBS` (re-exported from `@brazil-tms/shared`) into the worker's `JOB` / `JobName` / `JobPayloads` surface and into the `setupQueues` loop so the `billing.export` and `documents.checks` queues are created at bootstrap — per data-model §12 / R11 (depends on T012)
- [X] T042 [P] Add `DOCUMENTS_BUCKET=documents`, `EXPORTS_BUCKET=billing-exports`, `DOCUMENT_MAX_BYTES=10485760`, `DOCUMENT_CHECKS_CRON=*/5 * * * *` to `workers/.env` (local dev), `apps/web/.env.local.example`, and the `worker` service `environment:` block in `infra/supabase/docker-compose.yml` (beside `IMPORT_BUCKET`/`SLA_SWEEP_CRON`) — per data-model §15 / quickstart (depends on T001)
- [X] T043 [P] Ensure the `documents` and `billing-exports` Storage buckets exist (an idempotent `createBucket` at setup / a documented `db:seed`-adjacent step, mirroring how the `imports` bucket is provisioned) — per data-model §15 / research R8 (depends on T025)

### Foundational tests (Vitest unit — pure, no DB)

- [X] T044 [P] Unit test `packages/shared/src/domain/documents.test.ts`: `evaluateChecklist` (completion-missing vs billing-missing sets given the satisfied set), `DEFAULT_DOCUMENT_CHECKLIST` shape, `DOCUMENT_VERIFICATION_STATUSES` const (depends on T002)
- [X] T045 [P] Unit test `packages/shared/src/domain/billing.test.ts`: `computeBillingValues` (discounts subtracted, `manual_adjustment` signed, planned/executed/adjustment/final; null base ⇒ null final), `evaluateCompletionReadiness` (§19.3 blockers incl. `not_unloaded`/`missing_completion_documents`/`cancelled`), `evaluateBillingReadiness` (§19.4 blockers incl. `missing_billing_documents`/`no_pricing`/`open_billing_dispute`), and the `BILLING_ADJUSTMENT_TYPES`/`EXPORT_FORMATS`/`EXPORT_BATCH_STATUSES` consts (depends on T003)
- [X] T046 [P] Create Vitest schema tests `packages/shared/src/schemas/{document,document-requirement,rate,billing}.test.ts`: required/optional fields, `z.enum` membership, the `billingPeriod` `YYYY-MM` regex, the `waivedRequirements` array shape, length caps (depends on T007, T008, T009, T010)
- [X] T047 [P] Extend `packages/shared/src/audit/actions.test.ts`: `ALL_AUDIT_ACTIONS` contains the twelve new actions and stays in lockstep with the `AuditAction` union (depends on T006)
- [X] T048 [P] Extend `packages/shared/src/schemas/trip-board.test.ts`: the new `missingDocuments` param round-trips from `URLSearchParams` via `PARAM_KEYS` (depends on T011)
- [X] T049 [P] Extend `packages/shared/src/auth/permissions.test.ts`: `ROLE_PERMISSIONS`/`can` grant `upload_documents` to Admin/Ops-Mgr/Dispatcher/Control-Tower/Fleet-Coordinator/Finance, `verify_documents` to Admin/Ops-Mgr/Finance, `mark_completed` to Admin/Ops-Mgr/Control-Tower, `mark_billing_ready`/`edit_rates`/`export_billing` to Admin/Finance, and `manage_commercial_data` to Admin/Ops-Mgr (permission-matrix.md test focus — no new key)

**Checkpoint**: Schema applied; shared gates + billing compute + Zod + audit/jobs exported; Storage helpers + the cross-cutting reads/helpers (`resolveRequiredTypes`/`resolveRate`/`ensureBillingItem`/`loadBillingItemView`) + read-model extensions in place; the extended board/detail/dashboard routes return documents/billing data; nav + i18n + worker queue + env wired; foundational unit tests green. User stories can now begin.

---

## Phase 3: User Story 1 - Attach and verify proof-of-execution documents (Priority: P1) 🎯 MVP

**Goal**: An operations/finance user uploads a proof file (validated PDF/JPG/PNG ≤ ~10 MB → Supabase Storage, metadata in Postgres) with a type + external reference + notes (default `pending_review`), a reviewer marks it accepted/rejected/pending (recording verifier + timestamp), documents are downloadable via signed URL, and the Trip-Detail documents section + the Documents screen show the missing-document list against the customer checklist.

**Independent Test**: Upload a file with a type + reference → binary in Storage, row captures type/file/uploader/timestamp/reference/notes + `pending_review`; a `.exe`/oversize file → `UNSUPPORTED_FILE_TYPE`/`FILE_TOO_LARGE`, nothing stored; verify accepted/rejected → verifier+timestamp set; download via signed URL; the missing-document list reflects the checklist; a user without `upload_documents` cannot upload and without `verify_documents` cannot verify → `403`.

- [X] T050 [US1] Create `packages/db/src/documents/documents.ts` exporting `uploadDocument(tripId, { documentTypeId, fileStorageKey, externalReference, notes }, actor)` (resolve the active document type — unknown/inactive ⇒ `Conflict("INVALID_DOCUMENT_TYPE")`; INSERT `pending_review`; `writeAudit("document.upload")`), `verifyDocument(documentId, { verificationStatus, notes }, actor)` (set status + `verified_by_user_id`/`verified_at`; `writeAudit("document.verify")`), `archiveDocument(documentId, actor)` (set `archived_at`; `writeAudit("document.archive")`) — mirror `trip-transitions.ts`, each returns `loadTripDetail(tx, tripId)`; export from `packages/db/src/index.ts` (depends on T022, T029)
- [X] T051 [P] [US1] Create `apps/web/lib/trips/documents.ts` as a `"server-only"` re-export of `{ uploadDocument, verifyDocument, archiveDocument }` from `@brazil-tms/db` (depends on T050, T031)
- [X] T052 [US1] Create the POST handler `apps/web/app/api/trips/[id]/documents/route.ts` (upload): `requireAuth` → `requirePermission(ctx,"upload_documents")` → read `multipart/form-data` (`file` + `meta`), **validate** the file type against the allow-list (default PDF/JPG/PNG → else `409 UNSUPPORTED_FILE_TYPE`) and size against `DOCUMENT_MAX_BYTES` (else `409 FILE_TOO_LARGE`) **before** storing (R9), `uploadDocumentMetaSchema.parse(meta)`, `putDocument(documentStorageKey(...), Buffer.from(await file.arrayBuffer()), contentType)`, `uploadDocument(id, {...}, ctx.userId)` → `201 { item }`; `try/catch handleRouteError`; `dynamic="force-dynamic"`; export only POST (depends on T051, T032)
- [X] T053 [P] [US1] Create `apps/web/app/api/documents/[id]/route.ts`: PATCH (`requirePermission(ctx,"verify_documents")` + `verifyDocumentSchema.parse` → `verifyDocument`) and DELETE (`requirePermission(ctx,"upload_documents")` → `archiveDocument`) → `{ item }` (depends on T051)
- [X] T054 [P] [US1] Create the GET handler `apps/web/app/api/trips/[id]/documents/[docId]/download/route.ts`: `requirePermission(ctx,"view_all_trips")` → `signedUrl(file_storage_key, …)` → `{ url }` (404 for a missing doc / a waiver row with no file) (depends on T051, T032)
- [X] T055 [US1] Extend `apps/web/lib/trips/client.ts` with `useUploadDocument(id)` (multipart POST) / `useVerifyDocument` / `useArchiveDocument` mutation hooks (invalidate `["trips"]` + a new `["documents"]` root) and a `useDocumentDownloadUrl` helper — follow `useUpdateTripPlan`; reuse the 005 poll constants (depends on T052, T053, T054)
- [X] T056 [US1] Create `apps/web/components/trips/trip-detail/documents-section.tsx` (replaces `DocumentsPlaceholder`): the trip's document list + an upload form (type picker from `useDocumentTypes`/foundational list, external reference, notes), verify (accept/reject) actions, a **missing-document list** against the applicable checklist, and download links — wired to the US1 hooks, pt-BR labels/error mapping; wire it into `apps/web/components/trips/trip-detail/trip-detail-client.tsx` in place of `<DocumentsPlaceholder/>` and remove `DocumentsPlaceholder` from `placeholders.tsx` (leave the Billing placeholder for US2) (depends on T055, T037, T040)
- [X] T057 [US1] Create the Documents screen `apps/web/app/(shell)/documents/page.tsx` (server guard `view_all_trips`) + `apps/web/components/documents/document-list.tsx` (a trip/customer document view + the missing-document list, TanStack Table, pt-BR, 30 s polling) (depends on T055, T034)
- [X] T058 [P] [US1] Integration test `apps/web/lib/trips/documents.test.ts` (Vitest, static imports, `describe.skipIf(!DATABASE_URL)`, own seed + FK-safe cleanup): `uploadDocument` → row + `document.upload` audit; `verifyDocument` sets status/verifier/timestamp + `document.verify`; `archiveDocument` sets `archived_at` (soft-delete) + `document.archive`; `INVALID_DOCUMENT_TYPE` on an inactive type (depends on T050, T022)
- [X] T059 [P] [US1] Playwright e2e `apps/web/e2e/documents.spec.ts` (`apiLogin`): upload a document (type + reference) → appears with `pending_review`; a disallowed type / oversize file → `UNSUPPORTED_FILE_TYPE`/`FILE_TOO_LARGE`; verify accepted; download via signed URL; the missing-document list reflects the checklist; `upload_documents`/`verify_documents` holder `200/201` vs non-holder `403` (depends on T052, T053, T054, T056)

**Checkpoint**: US1 fully functional and independently testable — the proof-capture walking skeleton; the Trip-Detail documents section + missing-document list are live.

---

## Phase 4: User Story 2 - Validate completion and mark Billing Ready under enforced rules (Priority: P1)

**Goal**: An operations user marks a trip Completed only when §19.3 + the customer's completion-required documents are satisfied (auto-advancing to Billing Pending + creating the Billing Item); finance marks it Billing Ready only when §19.4 holds (billing docs + a rate-or-manual amount + no open dispute). A missing required document is cleared by an accepted upload OR an audited per-document waiver passed to the gated transition. The `documents.checks` sweep surfaces the two distinct §17 alerts + the "Completed trips missing documents" widget/view.

**Independent Test**: At `unloaded` with a completion-required doc missing → mark Completed blocked (`COMPLETION_BLOCKED`); supply the doc or a waiver → trip → `completed` → auto-advance `billing_pending` + Billing Item (period = month of completion, auto-priced from the seeded rate); with billing docs missing / no pricing / open dispute → mark Billing Ready blocked (`BILLING_READY_BLOCKED`); satisfy → `billing_ready`; every transition is a `transitionTripStatus` call; the sweep generates case-7 + case-8 alerts on their distinct predicates; users lacking `mark_completed`/`mark_billing_ready` → `403`.

- [X] T060 [US2] Create `packages/db/src/trips/completion.ts` exporting `markCompleted(tripId, input, actor)` and `markBillingReady(tripId, input, actor)` (mirror `trip-transitions.ts`): gather the gate context (current status; `resolveRequiredTypes(trip)` + the trip's accepted/waived satisfied set incl. `input.waivedRequirements`; for billing: `loadBillingItemView` `hasPricing` + `disputeStatus`), call the pure `evaluateCompletionReadiness`/`evaluateBillingReadiness` (blocked ⇒ `Conflict("COMPLETION_BLOCKED" | "BILLING_READY_BLOCKED")` with the missing types/blockers as `findings`), in the tx record any `input.waivedRequirements` as **waiver** `documents` rows (`waived_at`/`waived_reason`/`waived_by_user_id`, no file) + `writeAudit("document.waive")`, then `transitionTripStatus(...)` (reused — `unloaded→completed`; for completion immediately also `completed→billing_pending` (§11.6) + `ensureBillingItem`; for billing `billing_pending→billing_ready`); export both from `packages/db/src/index.ts` — per data-model §11 / R7 (depends on T028, T026, T003)
- [X] T061 [P] [US2] Create `apps/web/lib/trips/billing.ts` as a `"server-only"` re-export of `{ markCompleted, markBillingReady, loadBillingItemView }` from `@brazil-tms/db` (US4 extends this file with the billing-item mutations) (depends on T060)
- [X] T062 [US2] Create the POST handler `apps/web/app/api/trips/[id]/complete/route.ts`: `requirePermission(ctx,"mark_completed")` + `markCompletedSchema.parse` → `markCompleted(id, input, ctx.userId)` → `{ item }` (`409 COMPLETION_BLOCKED` with `findings`; `409 ILLEGAL_TRANSITION`/`STALE_TRANSITION` from the reused service); export only POST + `dynamic` (depends on T061)
- [X] T063 [P] [US2] Create the POST handler `apps/web/app/api/trips/[id]/billing-ready/route.ts`: `requirePermission(ctx,"mark_billing_ready")` + `markBillingReadySchema.parse` → `markBillingReady` → `{ item }` (`409 BILLING_READY_BLOCKED`) (depends on T061)
- [X] T064 [US2] Create `workers/jobs/document-checks/index.ts` exporting `runDocumentChecks()` + `registerDocumentChecks(boss)` (mirror `sla-sweep`): over **billing-phase trips** (`current_status ∈ {billing_pending, billing_ready}`), per trip in try/catch fault isolation (skip-and-continue), evaluate **two distinct per-case conditions** and generate/auto-resolve **each independently** via the **007 `alerts` store** — **case 7 `completed_missing_documents`** = the trip has ≥1 unmet required document of any kind; **case 8 `billing_blocked_missing_proof`** = `evaluateBillingReadiness` returns the `missing_billing_documents` blocker (blocked *specifically* by missing required-for-billing proof, NOT merely `no_pricing`/`open_billing_dispute`) — `generateAlert`/`autoResolveAlert` per case (`ON CONFLICT (alerts_trip_case_open_uq) DO NOTHING`); emit a per-sweep summary log; `registerDocumentChecks` calls `boss.schedule(DOCUMENT_JOBS.documentChecks, process.env.DOCUMENT_CHECKS_CRON ?? '*/5 * * * *', {}, {})` (the **second** scheduled job) — per data-model §12 / R11 / COV-001 (depends on T041, T026, T028)
- [X] T065 [US2] Register the sweep in `workers/jobs/index.ts` (`await registerDocumentChecks(boss)` in `registerJobHandlers`) (depends on T064)
- [X] T066 [US2] Extend `apps/web/lib/trips/client.ts` with `useMarkCompleted(id)` + `useMarkBillingReady(id)` mutation hooks (invalidate `["trips"]`; map `COMPLETION_BLOCKED`/`BILLING_READY_BLOCKED` findings) (depends on T062, T063)
- [X] T067 [US2] Create `apps/web/components/trips/trip-detail/billing-section.tsx` (replaces `BillingPlaceholder`): the billing values (read-only at this stage, from `billing` on the detail) + **Mark Completed / Mark Billing Ready** actions with a blockers display and a waiver input (document type + reason) wired to the US2 hooks, pt-BR labels/error mapping; wire it into `trip-detail-client.tsx` in place of `<BillingPlaceholder/>` and remove `BillingPlaceholder` from `placeholders.tsx` (US4 extends this section with the editable rate/adjustment controls) (depends on T066, T037, T040)
- [X] T068 [P] [US2] Wire `apps/web/components/trips/dashboard/widgets.tsx` to render the filled `completedMissingDocuments` count (deep-link to the `"missing_documents"` view; the `metric()` helper auto-flips placeholder→value) and add the **"Missing documents"** view to `apps/web/components/trips/control-tower-table.tsx` (sourced from `views.ts`) (depends on T038, T035)
- [X] T069 [P] [US2] Integration test `apps/web/lib/trips/completion.test.ts` (static imports, `skipIf(!DATABASE_URL)`): `markCompleted` blocked when a completion-required doc is missing (`COMPLETION_BLOCKED`); a waiver in `input.waivedRequirements` satisfies it (`document.waive` audit, waiver row, no file) and the trip transitions `unloaded→completed→billing_pending` with a `billing_items` row (period = month of completion, auto-priced from a seeded rate); `markBillingReady` blocked on missing billing docs / no pricing / `dispute_status='open'` (`BILLING_READY_BLOCKED`) and succeeds when satisfied; every transition is a `transitionTripStatus` status_change + `trip.status_change` audit (depends on T060, T022, T024)
- [X] T070 [P] [US2] Worker test `workers/jobs/document-checks/document-checks.test.ts` (`skipIf(!DATABASE_URL)`): **case 7** fires on a billing-phase trip with any unmet required doc; **case 8** fires only when `evaluateBillingReadiness` returns `missing_billing_documents` (NOT on `no_pricing`/`open_billing_dispute` alone) — the two are **independent**; idempotent generation (`ON CONFLICT` — re-run no duplicate); auto-resolve when a case clears; per-trip fault isolation (one bad trip skipped, sweep continues) (depends on T064, T022)
- [X] T071 [US2] Playwright e2e `apps/web/e2e/completion-billing.spec.ts` (`apiLogin`): mark Completed blocked with a missing doc → waive with a reason → completes + auto-advances to Billing Pending; mark Billing Ready blocked then passes; `mark_completed`/`mark_billing_ready` holder `200` vs non-holder `403`; the "Completed trips missing documents" widget + "Missing documents" view reflect a billing-phase trip with missing proof (depends on T062, T063, T067, T068)

**Checkpoint**: US1 + US2 = proof capture + enforced completion/Billing-Ready gating (on defaults) + the §17 missing-doc alerts. The Phase-4 close-out skeleton.

---

## Phase 5: User Story 3 - Configure per-customer required-document checklists (Priority: P2)

**Goal**: An Operations Manager (`manage_commercial_data`) configures, per customer, which document types are required and whether each blocks Completed and/or Billing Ready (optionally scoped by lane/vehicle type), and maintains the document-type master; a customer with no checklist runs on `DEFAULT_DOCUMENT_CHECKLIST` and is reported document-checklist sign-off blocked.

**Independent Test**: Create a checklist (one type completion-blocking, another billing-blocking, optionally scoped) → US2's gating uses it for that customer's trips; a customer with no rows falls back to the default and is reported sign-off blocked; a user without `manage_commercial_data` cannot edit → `403`.

- [X] T072 [US3] Extend `packages/db/src/documents/requirements.ts` with the CRUD `createDocumentRequirement`/`updateDocumentRequirement` (+ `document_requirement.create`/`update` audit) and `createDocumentType`/`updateDocumentType` (+ `document_type.create`/`update` audit) — the foundational reads `resolveRequiredTypes`/`list*` (T026) already exist; export the CRUD from `packages/db/src/index.ts` — per data-model §11 (depends on T026, T031)
- [X] T073 [P] [US3] Create `apps/web/lib/trips/document-requirements.ts` as a `"server-only"` re-export of `{ createDocumentRequirement, updateDocumentRequirement, listDocumentRequirements, createDocumentType, updateDocumentType, listDocumentTypes }` from `@brazil-tms/db` (depends on T072, T031)
- [X] T074 [P] [US3] Create `apps/web/app/api/document-types/route.ts` (GET `view_all_trips` → `listDocumentTypes`; POST `manage_commercial_data` + `createDocumentTypeSchema` → `createDocumentType` → `201`) and `apps/web/app/api/document-types/[id]/route.ts` (PATCH `manage_commercial_data` → `updateDocumentType`) (depends on T073)
- [X] T075 [P] [US3] Create `apps/web/app/api/document-requirements/route.ts` (GET `view_all_trips` `?customerId=` → `listDocumentRequirements`; POST `manage_commercial_data` + `createDocumentRequirementSchema` → `createDocumentRequirement` → `201`) and `apps/web/app/api/document-requirements/[id]/route.ts` (PATCH `manage_commercial_data` → `updateDocumentRequirement`) (depends on T073)
- [X] T076 [US3] Extend `apps/web/lib/trips/client.ts` with `useDocumentTypes()`/`useDocumentRequirements(customerId)` query hooks + `useCreateDocumentType`/`useUpdateDocumentType`/`useCreateDocumentRequirement`/`useUpdateDocumentRequirement` mutations (invalidate `["document-requirements"]`/`["document-types"]`) (depends on T074, T075)
- [X] T077 [US3] Create the admin screen `apps/web/app/(shell)/admin/document-requirements/page.tsx` (server guard — redirect non-`manage_commercial_data` users; loads customers/lanes/types options) + `apps/web/components/documents/requirement-admin.tsx` + `apps/web/components/documents/document-type-admin.tsx` (per-customer checklist + the document-type master; pt-BR; surfaces "document-checklist sign-off blocked" for customers with no rows) (depends on T076, T034)
- [X] T078 [P] [US3] Integration test `apps/web/lib/trips/requirements.test.ts` (static imports, `skipIf(!DATABASE_URL)`): `createDocumentRequirement`/`updateDocumentRequirement` + `createDocumentType`/`updateDocumentType` (+ audit); `resolveRequiredTypes` applies unscoped + matching scoped rows **additively**; a customer with **no** rows ⇒ `DEFAULT_DOCUMENT_CHECKLIST` + reported sign-off blocked (depends on T072, T026, T022)
- [X] T079 [P] [US3] Playwright e2e `apps/web/e2e/document-requirements.spec.ts` (`apiLogin` as Ops Manager): create a checklist → US2's completion/billing gating uses it; a no-checklist customer falls back to the default + shows sign-off blocked; `manage_commercial_data` holder `200` vs non-holder `403`; audit shows `document_requirement.*`/`document_type.*` (depends on T074, T075, T077)

**Checkpoint**: US1 + US2 + US3 — per-customer document checklists drive the gating; the document-type master is admin-managed.

---

## Phase 6: User Story 4 - Maintain simple rates and compute the billable breakdown (Priority: P2)

**Goal**: Finance (`edit_rates`) maintains rates (customer/lane/vehicle-type/effective-date, BRL cents); a completed trip's planned freight computes from the most-specific currently-effective rate; finance adds typed adjustments (each amount + note) so the trip shows planned/executed/adjustment/final; no matching rate ⇒ a manual amount + billing-rule sign-off blocked.

**Independent Test**: Create a rate → a matching trip auto-prices; add one of each adjustment type → final = executed + tolls + extras + penalties − discounts ± manual; planned/executed/adjustment/final all shown; a no-rate trip accepts a manual amount + reports billing-rule sign-off blocked; a user without `edit_rates` → `403`.

- [X] T080 [US4] Extend `packages/db/src/billing/rates.ts` with the CRUD `createRate`/`updateRate` (+ `rate.create`/`update` audit) — `resolveRate`/`listRates` (T027) already exist; export the CRUD from `packages/db/src/index.ts` — per data-model §11 (depends on T027, T031)
- [X] T081 [US4] Extend `packages/db/src/billing/billing-items.ts` with `updateBillingItem(tripId, input, actor)` (manual `base_freight_cents`/`billing_period`/`dispute_status`/`notes` + `billing_item.update` audit), `addBillingAdjustment(tripId, input, actor)` (INSERT a `billing_adjustments` row + `billing_item.update`), `removeBillingAdjustment(adjustmentId, actor)` (**soft-remove** — set `removed_at`/`removed_by_user_id`, never hard-delete — + `billing_item.update`) — `ensureBillingItem`/`loadBillingItemView` (T028) already exist; export the mutations from `packages/db/src/index.ts` — per data-model §6/§11 / R6 (depends on T028, T003)
- [X] T082 [P] [US4] Create `apps/web/lib/trips/rates.ts` as a `"server-only"` re-export of `{ createRate, updateRate, listRates }` from `@brazil-tms/db`, and **extend** `apps/web/lib/trips/billing.ts` (the US2 file) to also re-export `{ updateBillingItem, addBillingAdjustment, removeBillingAdjustment }` (depends on T080, T081, T061)
- [X] T083 [P] [US4] Create `apps/web/app/api/rates/route.ts` (GET `view_all_trips` → `listRates`; POST `edit_rates` + `createRateSchema` → `createRate` → `201`) and `apps/web/app/api/rates/[id]/route.ts` (PATCH `edit_rates` → `updateRate`) (depends on T082)
- [X] T084 [P] [US4] Create the PATCH handler `apps/web/app/api/trips/[id]/billing/route.ts`: `requirePermission(ctx,"edit_rates")` + `updateBillingItemSchema.parse` → `updateBillingItem` → `{ item: BillingItemView }` (`404` if no billing item yet) (depends on T082)
- [X] T085 [P] [US4] Create `apps/web/app/api/trips/[id]/billing/adjustments/route.ts` (POST `edit_rates` + `addBillingAdjustmentSchema` → `addBillingAdjustment`) and `apps/web/app/api/billing-adjustments/[id]/route.ts` (DELETE `edit_rates` → `removeBillingAdjustment` soft-remove) → `{ item: BillingItemView }` (depends on T082)
- [X] T086 [US4] Extend `apps/web/lib/trips/client.ts` with `useRates()`/`useCreateRate`/`useUpdateRate` and `useUpdateBillingItem(id)`/`useAddBillingAdjustment(id)`/`useRemoveBillingAdjustment` mutation hooks (invalidate `["trips"]`/`["rates"]`) (depends on T083, T084, T085)
- [X] T087 [US4] Extend `apps/web/components/trips/trip-detail/billing-section.tsx` (the US2 file): add the editable manual-amount control, the typed-adjustment add/remove controls (toll/waiting-time/redelivery/extra-stop/penalty/discount/manual, each amount + note), and the **planned / executed / adjustment / final billable** display in BRL — surfaces "billing-rule sign-off blocked" when no rate matched (depends on T086, T067)
- [X] T088 [US4] Create the rate admin screen `apps/web/app/(shell)/billing/rates/page.tsx` (server guard — redirect non-`edit_rates` users; loads customers/lanes options) + `apps/web/components/billing/rate-admin.tsx` (list + create/edit: customer/lane/vehicle-type/effective-dates/base amount; pt-BR; the optional toll/waiting/extra rule fields labeled as gated §29 Input #5) (depends on T086, T034)
- [X] T089 [P] [US4] Integration test `apps/web/lib/trips/rates.test.ts` (static imports, `skipIf(!DATABASE_URL)`): `resolveRate` precedence (lane+vehicle-type > single-scope > customer-default, tie-break latest `effective_start`; a rate outside its effective window not selected; no match ⇒ null); `createRate`/`updateRate` + `rate.*` audit (depends on T080, T027, T022)
- [X] T090 [P] [US4] Integration test `apps/web/lib/trips/billing-items.test.ts` (static imports, `skipIf(!DATABASE_URL)`): `ensureBillingItem` auto-prices from a matching rate (else null); `updateBillingItem` sets a manual base; `addBillingAdjustment` + `computeBillingValues` (discounts subtracted, final correct); `removeBillingAdjustment` **soft-removes** (`removed_at` set) and `loadBillingItemView` reads only live rows; `billing_item.update` audit (depends on T081, T028, T022)
- [X] T091 [US4] Playwright e2e `apps/web/e2e/rates-billing.spec.ts` (`apiLogin` as Finance): create a rate → a matching trip auto-prices; add adjustments → planned/executed/adjustment/final update; a no-rate trip accepts a manual amount + shows billing-rule sign-off blocked; `edit_rates` holder `200` vs non-holder `403` (depends on T083, T084, T085, T087, T088)

**Checkpoint**: US1–US4 — proof, gating, checklists, and the full rate + billable breakdown are live.

---

## Phase 7: User Story 5 - Export billing-ready trips and record export-batch history (Priority: P2)

**Goal**: Finance (`export_billing`) works the Billing pending/ready lists (filter customer + period), triggers a CSV/XLSX export (heavy generation on the worker → Supabase Storage + a durable `export_batches` record), the included trips are marked Billed (configurable), and the Billing screen shows export-batch history with downloadable files.

**Independent Test**: Seed billing-ready trips → the pending/ready lists show them; trigger an export → a `billing.export` worker job writes the file to Storage + an `export_batches` row (trip count/total/status), the trips become `billed` (configurable), re-export excludes already-billed (no double-bill); export uses the labeled default columns + reports export sign-off blocked until §29 Input #4; export-batch history lists + downloads; a user without `export_billing` → `403`.

- [X] T092 [US5] Create `packages/db/src/billing/export.ts` exporting `createExportBatch({ customerId, billingPeriod, format }, actor)`: verify a non-empty billing-ready set (else `Conflict("NO_BILLABLE_TRIPS")`), INSERT an `export_batches` row (`status='queued'`) + `writeAudit("billing.export")`, **enqueue** the `billing.export` job via `apps/web/lib/billing/queue.ts` (R11), return the batch; export from `packages/db/src/index.ts` — per data-model §11 (depends on T030, T033)
- [X] T093 [US5] Create `workers/jobs/billing-export/index.ts` exporting `runBillingExport({ exportBatchId, actorUserId })` + `registerBillingExport(boss)` (mirror the import-job convention; **on-demand**, no schedule): set `running` → load the batch's `customer_id`+`billing_period` → select the **billing-ready** trips + their `computeBillingValues` → generate the file with **ExcelJS** (`.xlsx.writeBuffer()` or `.csv.writeBuffer()` per `format` — **no new dep**) → `putExport(exportStorageKey(...), bytes, …)` → set `file_storage_key`/`trip_count`/`total_amount_cents`/status `completed` → `transitionTripStatus(trip, { toStatus:'billed', expectedFromStatus:'billing_ready' }, actor)` per included trip (configurable lock/flag; default lock) + link `billing_items.export_batch_id`; a throw ⇒ set the batch `failed` + `error_message` (durable status); idempotent retry skips already-`billed` trips — per data-model §12 / R11 (depends on T041, T030, T025, T028)
- [X] T094 [US5] Register the job in `workers/jobs/index.ts` (`await registerBillingExport(boss)` in `registerJobHandlers`) (depends on T093)
- [X] T095 [P] [US5] Create `apps/web/lib/billing/exports.ts` as a `"server-only"` re-export of `{ createExportBatch, queryBillingList, queryExportBatches }` from `@brazil-tms/db` (depends on T092, T030)
- [X] T096 [P] [US5] Create the GET handler `apps/web/app/api/billing/route.ts` (billing pending/ready lists): `requirePermission(ctx,"view_all_trips")` + `?scope=pending|ready&customerId=&period=` → `queryBillingList` → `{ items }` (FR-019) (depends on T095)
- [X] T097 [P] [US5] Create `apps/web/app/api/billing/exports/route.ts` (GET history `view_all_trips` → `queryExportBatches` → `{ items }`; POST `export_billing` + `createExportSchema` → `createExportBatch` → `202 { item }`, `409 NO_BILLABLE_TRIPS`) and `apps/web/app/api/billing/exports/[id]/download/route.ts` (GET `export_billing` → `signedUrl(file_storage_key, …)` → `{ url }`; `404` if not `completed`) (depends on T095, T032)
- [X] T098 [US5] Extend `apps/web/lib/trips/client.ts` with `useBillingList(scope, filters)`/`useExportBatches(filters)` query hooks + `useCreateExport()` mutation (invalidate `["billing"]`/`["trips"]`) (depends on T096, T097)
- [X] T099 [US5] Create the Billing screen `apps/web/app/(shell)/billing/page.tsx` (server guard `view_all_trips`) + `apps/web/components/billing/billing-lists.tsx` (pending + ready lists, filter customer + period, computed billable value + missing-proof indicator) + `apps/web/components/billing/export-panel.tsx` (export action — customer/period/format — + export-batch history with download; notes the labeled default columns + export sign-off blocked until §29 Input #4), pt-BR, 30 s polling (depends on T098, T034)
- [X] T100 [P] [US5] Integration test `apps/web/lib/billing/export.test.ts` (static imports, `skipIf(!DATABASE_URL)`): `createExportBatch` guards `NO_BILLABLE_TRIPS`, inserts a `queued` batch + `billing.export` audit + enqueues; `queryBillingList` returns pending vs ready sets with the computed value + missing-proof flag; `queryExportBatches` history (depends on T092, T030, T022)
- [X] T101 [P] [US5] Worker test `workers/jobs/billing-export/billing-export.test.ts` (`skipIf(!DATABASE_URL)`): `runBillingExport` selects billing-ready trips → ExcelJS buffer for both `xlsx` and `csv` → `putExport` → batch `completed` + `trip_count`/`total_amount_cents`; each included trip → `billed` (+ `export_batch_id` linked); a thrown error → batch `failed` + `error_message`; idempotent retry skips already-`billed` trips (no double-bill) (depends on T093, T022)
- [X] T102 [US5] Playwright e2e `apps/web/e2e/billing-export.spec.ts` (`apiLogin` as Finance): the pending/ready lists filter by customer + period with missing-proof indicators; trigger an export → batch appears in history + downloads; included trips become Billed; re-export of the same period does not double-bill; `export_billing` holder `202`/`200` vs non-holder `403` (depends on T096, T097, T099)

**Checkpoint**: All five user stories independently functional; finance can close out, price, and export billing-ready trips with durable batch history.

---

## Phase 8: Polish & Cross-Cutting Concerns

- [X] T103 [P] Add the no-dotted-key + audit-action completeness assertions for the twelve new actions to `apps/web/lib/messages.test.ts` (every `ALL_AUDIT_ACTIONS` entry has a flat `AuditActions` key via `action.replaceAll(".","_")`; the nested `Trips.auditActions.{document,document_requirement,document_type,rate,billing_item,billing}` entries resolve; no key contains `.`) (depends on T040, T006)
- [X] T104 Run `pnpm exec vitest run --project web apps/web/lib/messages.test.ts` and fix any missing/dotted-key failures in `apps/web/messages/pt-BR.json` (a dotted i18n key breaks ALL authenticated page renders, MEMORY) (depends on T103)
- [X] T105 [P] Authz matrix e2e `apps/web/e2e/documents-billing-authz.spec.ts` (`apiLogin`): `upload_documents` holder `201` vs non-holder `403` on upload; `verify_documents` `200` vs `403` on verify; `mark_completed` `200` vs `403` on complete; `mark_billing_ready` `200` vs `403` on billing-ready; `edit_rates` `200` vs `403` on rate/billing edit; `export_billing` `202` vs `403` on export; `manage_commercial_data` `200` vs `403` on document requirements/types; view-only roles read (`GET /api/trips/:id`, `/api/billing`, `/api/billing/exports`, `/api/dashboard/summary` → `200`) via `view_all_trips`; unauthenticated → `401` (depends on T052, T053, T062, T063, T074, T075, T083, T084, T097)
- [X] T106 [P] Add a performance-sanity note to `specs/008-documents-billing-export/quickstart.md`: the pure gates + `computeBillingValues` sub-ms; Documents screen + Billing lists + Trip-Detail documents/billing sections `< ~3 s` at medium scale via the new indexes; the billing export over a customer+period batch completes on the worker within a configurable soft target; the ~5-min `documents.checks` sweep within cadence — manual spot-check, not a perf harness (validates SC-010) (depends on T064, T093, T099)
- [X] T107 Run the quickstart.md US1–US5 validation against a fresh build with both the app and the worker running and the Storage buckets provisioned (`pnpm db:seed` + `db:seed:master-data` + `db:seed:trip-domain` + `db:seed:document-types` + `db:seed:rates`, then `db:seed:e2e` to reset accounts) (depends on T059, T071, T079, T091, T102)
- [X] T108 Run the quality gates from repo root and fix failures: `pnpm lint`; `pnpm typecheck`; `pnpm build`; `pnpm test` (with `DATABASE_URL` set; web integration via `pnpm exec vitest run --project web`; e2e against a prod build, `--workers=1`) — targeting the `dev` branch (depends on T104, T107)
- [X] T109 Open the 008 PR to `dev` via `gh pr create --base dev` using the PR template, noting: first enforcement of `upload_documents`/`verify_documents`/`mark_completed`/`mark_billing_ready`/`edit_rates`/`export_billing` + reuse of `manage_commercial_data`; **seven** new tables + **three** enums + **two** Storage buckets + **two** worker jobs (no new permission key/package/worker process/runtime dep/`trips` ALTER); completion/Billing-Ready are transitions on the reused 003 machine, billing status is the `billingStatus` projection; the two §17 cases lit up via the 007 `alerts` store; `trip_events`/`audit_logs` stay append-only and the seven tables are soft-deleted/retained; and the gated/deferred items (per-customer required documents §29 #3 + sign-off blocked, finance export format #4, per-customer billing rules #5) — AI does NOT merge to `main` (depends on T108)

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (T001)**: none.
- **Foundational (T002–T049)**: depends on Setup — BLOCKS all user stories. Internal order: shared domain/jobs/audit/schemas (T002–T011) before the shared barrel (T012); the enum extend + seven schema files (T013–T020) — note `documents` (T015) after `document_types` (T014); `billing_items` (T019) after `export_batches` (T018); `billing_adjustments` (T020) after `billing_items` (T019) — before the schema barrel (T021) before generate/apply migration (T022); seeds (T023–T024) after the migration; the Storage helpers (T025) + the cross-cutting reads/helpers (T026 `resolveRequiredTypes`, T027 `resolveRate`, T028 `ensureBillingItem`/`loadBillingItemView` after T027) + read-model extends (T029 after T028/T026, T030 after T028/T026) before the db barrel (T031); web Storage re-export + enqueue client + read re-exports + nav + i18n (T032–T040); worker queue + env + buckets (T041–T043); foundational unit tests (T044–T049) after the code they cover.
- **US1 (T050–T059, P1)**: after Foundational. The proof-capture MVP.
- **US2 (T060–T071, P1)**: after Foundational (uses the gate evaluators T003, `resolveRequiredTypes` T026, `ensureBillingItem`/`loadBillingItemView` T028, `transitionTripStatus` reuse, the 007 alert helpers). Independently testable on the default checklist + the seeded sample rate.
- **US3 (T072–T079, P2)**: after Foundational (extends `requirements.ts` T026). Configures the checklists US2 consumes (US2 runs on defaults without it).
- **US4 (T080–T091, P2)**: after Foundational (extends `rates.ts` T027 + `billing-items.ts` T028) and after US2 (extends `billing.ts` T061 + `billing-section.tsx` T067).
- **US5 (T092–T102, P2)**: after Foundational (reads `queryBillingList`/`queryExportBatches` T030, enqueue client T033) and US4 (priced billing-ready trips to export).
- **Polish (T103–T109)**: after the stories it validates.

### Story Independence

- US1 is independently shippable (proof capture + verify + missing list). US2 adds enforced completion/Billing-Ready gating + the §17 missing-doc alerts (on the default checklist + seeded rate). US3 makes the checklist per-customer-configurable (US2 runs on defaults without it). US4 adds the rate + billable breakdown. US5 adds the export + history. Each adds value without breaking earlier stories.

### Parallel Opportunities

- **Foundational**: T002–T011 (different shared files) run in parallel; T014/T016/T017/T018 parallel, then T015→(T014) and T019→(T018) and T020→(T019) serialize on their FKs; T021→T022 (barrel→migration) serialize; T023/T024 parallel after T022; T025 parallel; T026/T027 parallel after T022, then T028 after T027, T029/T030 after T028; T031 after; T032–T040 parallelize once their deps land; T041–T043 parallel; T044–T049 (unit tests) all parallel.
- Service-file tasks that touch one file are **sequential**: `apps/web/lib/trips/client.ts` (T055, T066, T076, T086, T098); `packages/db/src/documents/requirements.ts` (T026 then T072); `packages/db/src/billing/rates.ts` (T027 then T080); `packages/db/src/billing/billing-items.ts` (T028 then T081); `apps/web/lib/trips/billing.ts` (T061 then T082); `apps/web/components/trips/trip-detail/billing-section.tsx` (T067 then T087); the trip-detail `placeholders.tsx`/`trip-detail-client.tsx` edits (T056, T067).
- **US1**: T053 ∥ T054 (different route files) after T051; T058 ∥ T059 after impl deps.
- **US3**: T074 ∥ T075 (different route files) after T073; T078 ∥ T079.
- **US4**: T083 ∥ T084 ∥ T085 (different route files) after T082; T089 ∥ T090.
- **US5**: T096 ∥ T097 (different route files) after T095; T100 ∥ T101.
- Different user stories can be staffed in parallel once Foundational completes (mind the shared-file serializations above).

---

## Parallel Example: Foundational shared layer

```bash
# Different shared files — launch together:
Task: "Pure documents module packages/shared/src/domain/documents.ts"      # T002
Task: "Pure billing gates + compute packages/shared/src/domain/billing.ts" # T003
Task: "Billing job contract packages/shared/src/billing/jobs.ts"           # T004
Task: "Documents job contract packages/shared/src/documents/jobs.ts"       # T005
Task: "Audit actions extend packages/shared/src/audit/actions.ts"          # T006
Task: "Zod schemas document/document-requirement/rate/billing"             # T007–T010
Task: "trip-board schema extend (missingDocuments + PARAM_KEYS)"           # T011
```

---

## Implementation Strategy

### MVP First

1. Complete Phase 1 (Setup) + Phase 2 (Foundational).
2. Complete Phase 3 (US1) → **STOP and VALIDATE**: documents uploadable/verifiable/downloadable, the Trip-Detail documents section + missing-document list live. The proof-capture walking skeleton.
3. Complete Phase 4 (US2) → enforced completion/Billing-Ready gating on the default checklist + the §17 missing-doc alerts (the two P1 stories). **Recommended MVP boundary** — the Phase-4 "upload proof docs; mark billing pending; validate billing-ready" exit bar (§22/§23) on configurable defaults.

### Incremental Delivery

US3 (per-customer checklists) → US4 (rates + billable breakdown) → US5 (export + history) → Polish (i18n guard / authz matrix / perf / quickstart / quality gates / PR). Each story is demoable and independently testable.

### Parallel Team Strategy

After Foundational: one developer takes US1, another US2 (US2 runs on the default checklist + seeded rate). US3 follows (or parallels) as the checklist-config layer; US4 follows once US2's `billing.ts`/`billing-section.tsx` land; US5 follows US4's priced trips. Respect the shared-file serializations (`client.ts`, `requirements.ts`, `rates.ts`, `billing-items.ts`, `billing.ts`, `billing-section.tsx`, the trip-detail placeholders/client).

---

## Notes

- Tests are included (required by plan Testing / Constitution §3.13 / quickstart). Pure correctness → Vitest `packages/shared` unit (the completion/billing-readiness gates + `computeBillingValues` + Zod); services + read models + worker jobs → Vitest integration with `DATABASE_URL` (static imports, `skipIf(!DATABASE_URL)`); HTTP statuses (401/403/400/404/409) + finding/error payloads + UI flows + the authz matrix → Playwright `e2e/` (the project has **no** `route.test.ts` — web Vitest only includes `lib/**`, MEMORY).
- `[P]` = different files, no incomplete dependency. The shared-file serializations are listed under Parallel Opportunities — honor them.
- **No new permission key** (first-enforce `upload_documents`/`verify_documents`/`mark_completed`/`mark_billing_ready`/`edit_rates`/`export_billing`, reuse `manage_commercial_data`; reads + document download stay on `view_all_trips`, export-file download on `export_billing`), **no new package**, **no new worker process** (the existing worker gains one on-demand + the second scheduled job), **no new runtime dependency** (ExcelJS emits CSV+XLSX), **no `trips` ALTER** (billing status is the `billingStatus(current_status)` projection — FR-011). Seven new tables; three enums; `document_types` a config table; `export_batches.format`/`billing_items.dispute_status` CHECK text.
- Completion/billing-readiness authority is server-side (the pure evaluators the BFF calls — the UI never decides); completion/Billing-Ready/Billed are transitions on the reused 003 status machine; billing values are a computed projection (`computeBillingValues`), never stored; binaries in Supabase Storage (metadata in Postgres, signed-URL download); `trip_events`/`audit_logs` stay append-only; the seven tables mutate but are soft-deleted/retained, never hard-deleted (documents `archived_at`, adjustments `removed_at`, export batches retained); freshness is polling (no Realtime); UI is pt-BR with no dotted i18n keys; timestamps UTC; money integer centavos (BRL).
- The two §17 cases are generated on **distinct predicates** (data-model §12): case 7 = any unmet required document; case 8 = the `missing_billing_documents` billing-readiness blocker.
- Gated/deferred (configurable defaults, not invented — Constitution II): per-customer required documents (§29 #3 — `DEFAULT_DOCUMENT_CHECKLIST` + per-customer document-checklist sign-off blocked), the finance export format (#4 — labeled default columns + export sign-off blocked), per-customer billing rules (#5 — manual values + billing-rule sign-off blocked).
- Commit after each task or logical group; AI does not merge to `main` (PRs target `dev`).
```
